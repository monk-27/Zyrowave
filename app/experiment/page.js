
const Experiment = ({ para, id, name, mob }) => (
  <div>
    <p>my paragraph is: {para}</p>
    <span>This is id:{id}</span>
    <h2>This is my {name}</h2>
    <p>my mob: {mob}</p>
  </div>
);

export default Experiment;
